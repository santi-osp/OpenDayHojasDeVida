{
    "name": "openday-cv-functions",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "main": "dist/src/index.js",
    "scripts": {
        "build": "tsc -p .",
        "typecheck": "tsc -p . --noEmit",
        "clean": "rimraf dist",
        "prestart": "npm run build",
        "start": "func start"
    },
    "dependencies": {
        "@azure/functions": "^4.9.0",
        "openai": "^4.104.0"
    },
    "devDependencies": {
        "@types/node": "^24.12.2",
        "rimraf": "^6.1.0",
        "typescript": "^5.9.3"
    }
}